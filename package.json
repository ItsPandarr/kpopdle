{
  "name": "kpopdle",
  "version": "0.1.0",
  "description": "Loldle-style K-pop group guessing game. Pure static site.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 scripts/serve.py 8123",
    "scrape": ".venv/bin/python scripts/scrape.py",
    "encode": "python3 scripts/encode_data.py",
    "sync-i18n": "node scripts/sync-i18n-en.mjs",
    "build": "node scripts/sync-i18n-en.mjs && node scripts/build.mjs",
    "preview": "node scripts/sync-i18n-en.mjs && node scripts/build.mjs && python3 -m http.server 8124 --directory dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
